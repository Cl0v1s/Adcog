{% extends 'AdcogBloggerBundle:BloggerSlider:read.html.twig' %}

{% block title %}{{ title(null,{'%entity%':slider}) }}{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
    <li>{{ link('blogger_slider_delete',{slider:slider.id},{},{vars:{'%entity%':slider }}) }}</li>
{% endblock %}

{% block subcontent %}
    <ul class="list-inline">
        <li>{{ link('blogger_slider_index',{},{class:'btn btn-xs btn-default',ficon:'arrow-left'}) }}</li>
        <li>{{ link('blogger_slider_create',{},{class:'btn btn-xs btn-success',ficon:'plus'}) }}</li>
        <li>{{ link('blogger_slider_read',{slider:slider.id},{class:'btn btn-xs btn-default',ficon:'tasks'},{vars:{'%entity%':slider }}) }}</li>
        <li>{{ link('blogger_slider_update',{slider:slider.id},{class:'btn btn-xs btn-warning',ficon:'pencil'},{vars:{'%entity%':slider }}) }}</li>
    </ul>
    <p class="alert alert-danger">Êtes-vous certain de voulois supprimer {{ slider }} ?</p>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="row form-group">
        <div class="col-lg-12">
            <ul class="list-inline">
                <li>{{ link('blogger_slider_index',{},{class:'btn btn-xs btn-default',ficon:'arrow-left'}) }}</li>
                <li>{{ link('blogger_slider_create',{},{class:'btn btn-xs btn-success',ficon:'plus'}) }}</li>
                <li>{{ link('blogger_slider_read',{slider:slider.id},{class:'btn btn-xs btn-default',ficon:'tasks'},{vars:{'%entity%':slider }}) }}</li>
                <li>{{ link('blogger_slider_update',{slider:slider.id},{class:'btn btn-xs btn-warning',ficon:'pencil'},{vars:{'%entity%':slider }}) }}</li>
                <li class="pull-right">
                    <button type="submit" class="btn btn-danger"><span class="fa fa-trash-o"></span> {{ title() }}</button>
                </li>
            </ul>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}