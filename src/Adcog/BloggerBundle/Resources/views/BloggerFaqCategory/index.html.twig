{% extends 'AdcogBloggerBundle:Blogger:index.html.twig' %}

{% block breadcrumb %}
    {{ parent() }}
    <li>{{ link('blogger_faq_category_index') }}</li>
{% endblock %}

{% block subcontent %}
    {% include 'AdcogDefaultBundle::filter.html.twig' %}
    <ul class="list-inline">
        <li>{{ link('blogger_faq_category_create',{},{class:'btn btn-xs btn-success',ficon:'plus'}) }}</li>
    </ul>
    <div class="row">
        <div class="table_responsive">
            <table class="{{ table_class }}">
                <thead>
                    <tr>
                        <th class="sm">#</th>
                        <th>Catégorie</th>
                        <th class="sm"></th>
                        <th class="sm"></th>
                        <th class="sm"></th>
                    </tr>
                </thead>
                <tbody>
                    {% for faqCategory in paginator %}
                        <tr>
                            <td>{{ faqCategory.id }}</td>
                            <td>{{ faqCategory }}</td>
                            <td>{{ link('blogger_faq_category_read',{faqCategory:faqCategory.id},{class:'btn btn-xs btn-default',ficon:'comment',name:''},{vars:{'%entity%':faqCategory }}) }}</td>
                            <td>{{ link('blogger_faq_category_update',{faqCategory:faqCategory.id},{class:'btn btn-xs btn-warning',ficon:'pencil',name:''},{vars:{'%entity%':faqCategory }}) }}</td>
                            <td>{{ link('blogger_faq_category_delete',{faqCategory:faqCategory.id},{class:'btn btn-xs btn-danger',ficon:'trash-o',name:''},{vars:{'%entity%':faqCategory }}) }}</td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="100%">
                                <p class="alert alert-info">Cette liste est vide ...</p>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}