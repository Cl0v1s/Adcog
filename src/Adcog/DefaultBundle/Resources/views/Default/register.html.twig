{% extends 'AdcogDefaultBundle::layout.html.twig' %}

{% block breadcrumb %}
    {{ parent() }}
    <li>{{ link('default_register') }}</li>
{% endblock %}

{% block content %}
    <div class="featurette">
        <h1 class="featurette-heading">Créez votre compte ! <span class="muted">C'est gratuit.</span></h1>
    </div>
    <div class="row">
        <div class="col-md-6">
            {{ form_start(form,{attr:{class:'form-horizontal' }}) }}
            {{ form_widget(form) }}
            <button class="btn btn-default btn-block" type="submit">Inscription</button>
            {{ form_end(form) }}
        </div>
        <div class="col-md-6">
            <h2>{{ go.title }}</h2>
            <div>{{ go.content|raw }}</div>
            <h2>{{ why.title }}</h2>
            <div>{{ why.content|raw }}</div>
            <h2>{{ more.title }}</h2>
            <div>{{ more.content|raw }}</div>
        </div>
    </div>
{% endblock %}