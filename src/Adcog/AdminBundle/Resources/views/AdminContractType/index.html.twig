{% extends 'AdcogAdminBundle:Admin:index.html.twig' %}

{% block breadcrumb %}
    {{ parent() }}
    <li>{{ link('admin_contract_type_index') }}</li>
{% endblock %}

{% block subcontent %}
    {% include 'AdcogDefaultBundle::filter.html.twig' %}
    <ul class="list-inline">
        <li>{{ link('admin_contract_type_create',{},{class:'btn btn-xs btn-success',ficon:'plus'}) }}</li>
    </ul>
    <div class="row">
        <div class="table_responsive">
            <table class="{{ table_class }}">
                <thead>
                    <tr>
                        <th class="sm">#</th>
                        <th></th>
                        <th class="sm"></th>
                        <th class="sm"></th>
                        <th class="sm"></th>
                        <th class="sm"></th>
                    </tr>
                </thead>
                <tbody>
                    {% for contract in paginator %}
                        <tr>
                            <td>{{ contract.id }}</td>
                            <td>{{ contract }}</td>
                            <td>{{ link('admin_contract_type_read',{contract:contract.id},{class:'btn btn-xs btn-default',ficon:'tasks',name:'',title:contract}) }}</td>
                            <td>{{ link('admin_contract_type_replace',{contract:contract.id},{class:'btn btn-xs btn-info',ficon:'random',name:'',title:contract}) }}</td>
                            <td>{{ link('admin_contract_type_update',{contract:contract.id},{class:'btn btn-xs btn-warning',ficon:'pencil',name:'',title:contract}) }}</td>
                            <td>{{ link('admin_contract_type_delete',{contract:contract.id},{class:'btn btn-xs btn-danger',ficon:'trash-o',name:'',title:contract}) }}</td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="100%">
                                <p class="alert alert-info">Cette liste est vide ...</p>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}