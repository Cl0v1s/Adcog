{% extends 'AdcogAdminBundle:Admin:index.html.twig' %}

{% block breadcrumb %}
    {{ parent() }}
    <li>{{ link('admin_sector_index') }}</li>
{% endblock %}

{% block subcontent %}
    {% include 'AdcogDefaultBundle::filter.html.twig' %}
    <ul class="list-inline">
        <li>{{ link('admin_sector_create',{},{class:'btn btn-xs btn-success',ficon:'plus'}) }}</li>
    </ul>
    <div class="row">
        <div class="table_responsive">
            <table class="{{ table_class }}">
                <thead>
                    <tr>
                        <th class="sm">#</th>
                        <th></th>
                        <th class="sm"></th>
                        <th class="sm"></th>
                        <th class="sm"></th>
                        <th class="sm"></th>
                    </tr>
                </thead>
                <tbody>
                    {% for sector in paginator %}
                        <tr>
                            <td>{{ sector.id }}</td>
                            <td>{{ sector }}</td>
                            <td>{{ link('admin_sector_read',{sector:sector.id},{class:'btn btn-xs btn-default',ficon:'tasks',name:'',title:sector}) }}</td>
                            <td>{{ link('admin_sector_replace',{sector:sector.id},{class:'btn btn-xs btn-info',ficon:'random',name:'',title:sector}) }}</td>
                            <td>{{ link('admin_sector_update',{sector:sector.id},{class:'btn btn-xs btn-warning',ficon:'pencil',name:'',title:sector}) }}</td>
                            <td>{{ link('admin_sector_delete',{sector:sector.id},{class:'btn btn-xs btn-danger',ficon:'trash-o',name:'',title:sector}) }}</td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="100%">
                                <p class="alert alert-info">Cette liste est vide ...</p>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}